services:
  - type: web
    name: evxchange-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn backend.run:app"
    envVars:
      - key: FLASK_ENV
        value: production
